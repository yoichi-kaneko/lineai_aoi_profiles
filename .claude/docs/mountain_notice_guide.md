# 山行連絡モードの共通手順（継灯・帰灯）

継灯（`stay_mountain`）・帰灯（`off_mountain`）が共通で用いる判断手順です。取得範囲・報告構成・中断時の扱いは各 `modes/*.md` を正とし、**本ドキュメントは「山行コンテキストの確定」と「時刻の採用優先順位」の正本**とします。

## 1. 山行コンテキストの確定

山名・コース・入山日・下山予定日を、次の優先順位で確定してください。

**（A）門灯モードの `up_mountain` 記録がある場合 — 計画書 fetch を省略**

Firestore に `type` = `up_mountain` の記録があり、今回の連絡がその山行に対応すると解釈できる場合は、その記録の山名・コース・下山予定日を「登山日程」として採用し、**`fetch_yamap_plan` は実行しない**でください。カレンダー上の予定と食い違う場合のみ、照合のために計画書 fetch を行います。

`up_mountain` の記録日は**本日でなくて構いません**。一泊以上の山行では門灯モードが入山日に実行されるため、**前日以前の `up_mountain` に本日の継灯・帰灯が対応する**のが通常の形です。日帰りなら入山日・下山日ともに本日、一泊以上なら入山予定日〜下山予定日が日程になります。

**（B）上記に該当しない場合 — 計画書 fetch**

カレンダーの予定から `https://yamap.com/plans/code/${CODE}/printing` 形式のURLを含む登山予定を探し、**`fetch_yamap_plan` スキル（計画書URL用）**で詳細（山名、入山日、下山日、コース）を取得してください。複数ある場合は、最も早い入山予定日〜最も遅い下山予定日を「取得日程」とし、後続の情報処理でどの山への連絡かを判断します。

**登山予定が見つからなかった場合の扱いはモードごとに異なります**（ユーザーへ確認を送るか、何も送らず終了するか）。各モードの手順に従ってください。

**備考：複数回の登山予定がある場合の前提**

連絡は **1回目の `up_mountain` → 1回目の `off_mountain` → 2回目の `up_mountain`** のように、開始連絡と終了連絡が交互に送られる前提です。Firestore 記録の `up_mountain` と `off_mountain` を照合し、**まだ対応する `off_mountain` がない最新の `up_mountain`** を今回の対象として優先してください。一泊以上の山行では両者が別々の日に記録されるため、照合は同日内に限らず**日跨ぎで**行います。

未対応の `up_mountain` が複数ある、`up_mountain` が無いのに終了連絡がトリガーされたなど、どの山行への連絡かを記録から正しく解釈できない場合は、機械的にカレンダーの予定を選ばず、**`user` 宛**に確認してください。誤った山行を家族へ報告しないためです。

## 2. 時刻の採用優先順位

家族へ伝える時刻（継灯＝宿泊地への到着時刻、帰灯＝下山時刻）は、以下の優先順位で決定し、**「〇時〇分頃」** のように丸めた表現で本文に含めてください。

1. **ユーザーが明示した時刻を最優先**: 本モードをトリガーした `line_text` に時刻の明示（例:「下山は11:30頃に終わっていました」「山小屋には14:00頃に着きました」）がある場合は、**Swarmや現在時刻より優先して**採用する。山中は電波が届かず、行動の区切りから連絡までに時間差が生じるため、ユーザー本人の申告が最も実態に近い。
   - 連絡が遅れた事情（電波不通など）が読み取れる場合、または当該時刻と連絡時刻に明らかな開きがある場合は、家族が時刻のずれを不審に思わないよう一言添える（例:「電波の届かない道が続いたため、私のもとに便りが届いたのは先ほどでした」）。
2. **Swarm のチェックイン時刻**: 明示がない場合、該当するチェックインがあればその時刻を採用する（帰灯モードでは `modes/off_mountain.md` の「下山地点の判定」を満たすチェックインに限る）。本文では採用根拠となった地点を明示する（例:「登山口に戻った時刻」「○○駅に到着した時刻」）。単に「下山した時刻」とだけ書かず、どの地点・局面の時刻かを家族が誤解なく読めるようにする。
3. **現在時刻をフォールバックとして使用**: いずれも該当しない場合は、報告作成ステップの冒頭で取得した現在時刻（碧衣が連絡を受けた時刻）を採用する。
