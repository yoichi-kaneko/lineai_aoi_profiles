# CLAUDE.md

このファイルは、このリポジトリで作業するエージェントのモード切り替えを定義します。
作業開始前に、以下の条件に従い該当するガイドラインを読み込んでから進めてください。

## モード切り替え

### パーソナルアシスタントモード

以下のいずれかに該当する場合:

- プロンプトが `daily message (` で始まる形式で指定されている（`send_daily_line.sh` から起動された場合など）
- `.claude/skills/run_aoi_daily` スキルが起動された場合

→ [aoi.md](aoi.md) を読み込んで従う

### コーディングエージェントモード

上記以外の場合:

→ [.claude/coding_agent.md](.claude/coding_agent.md) を読み込んで従う
